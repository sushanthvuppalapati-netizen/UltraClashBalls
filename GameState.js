/**
 * Game state enum representing all screens in the game lifecycle.
 */
const GameState = {
    CHAR_SELECT: "CHAR_SELECT",
    MAP_SELECT: "MAP_SELECT",
    PLAYING: "PLAYING",
    GAME_OVER: "GAME_OVER"
};